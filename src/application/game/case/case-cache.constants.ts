/** TTL for public case catalog reads (list + detail). */
export const CASE_PUBLIC_READ_CACHE_TTL_SECONDS = 120;

/** TTL for filtered list snapshots (many distinct keys; keep moderate). */
export const CASE_FILTERED_LIST_CACHE_TTL_SECONDS = 90;
